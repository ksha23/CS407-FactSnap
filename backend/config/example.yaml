name: "FactSnap"
environment: "local"

server:
  port: "8080"
  baseUrl: "api"

postgres:
  host: "localhost"
  port: "5432"
  user: "user"
  password: "pw"
  name: "server-db"
  sslmode: "disable"

clerk:
  secretKey:

s3:
  bucket:
  region:
  accessKeyId:
  secretAccessKey:
  sessionToken:
  usePathStyle: false  # Set to true for LocalStack/MinIO, can be overridden by S3_USE_PATH_STYLE env var
  presignDuration:
  cdnBaseUrl: